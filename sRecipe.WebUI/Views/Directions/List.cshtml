@model List<sRecipe.WebUI.Models.DirectionViewModel>
<h4>Directions</h4>
<table class="table">
    <thead>
        <tr>
            <th>
                Step
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Description)
            </th>
        </tr>
    </thead>
    @foreach (var item in Model)
    { 
        <tr>
            <td>
                @Html.DisplayFor(o => item.Order)
            </td>
              <td>
                @Html.DisplayFor(o => item.Description)
            </td>
        </tr>
    }

</table>

